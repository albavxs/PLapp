{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "jsx": "react",  // Habilita suporte a JSX
    "baseUrl": ".",  // Base para caminhos relativos
    "paths": {
      "@/*": [""] 
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
